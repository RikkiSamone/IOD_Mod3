const pizzaArray = ["start preparing pizza", "made the base", "added the sauce and cheese", "added the pizza toppings", "cooked the pizza", "pizza is ready"];


function pizzasteps(num) {
    if (num == 1) {
        return pizzaArray[0];
    } else if (num == 2) {
        return pizzaArray[1];
    } else if (num == 3) {
        
  }
}

console.log(pizzasteps(1));